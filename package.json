{
  "name": "sandbox-vite-app",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "turbo run build",
    "storybook": "turbo run storybook --filter=sandbox-vite-app-storybook-mui"
  },
  "devDependencies": {
    "prettier": "2.7.1",
    "turbo": "^1.4.3"
  }
}